[package]
name = "rsa-test"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ckb-std = "0.14.2"
rsa = { version = "0.9.2", default-features = false, features = ["pem", "u64_digit", "sha2"]}
